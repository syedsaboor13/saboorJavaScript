// for in loop

/* 
used to loop through the properties (keys) of an object values
*/
let mydetails = {
    name:"SyedSaboor",
    age:18,
    college:"MJ",
    isAlive: true,
    isSleeping : null,
    isAwake : undefined
}
for (let x in mydetails)
    console.log(mydetails(x));